import torch

def svd_2x2(A: list[list[float]]) -> tuple:
    """
    Computes the SVD of a 2x2 matrix A = U @ diag(S) @ Vt.

    Args:
        A: 2x2 matrix as a nested list

    Returns:
        Tuple of (U, S, Vt) each rounded to 4 decimal places
    """
    # Your code here
    pass
